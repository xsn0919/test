// 放在 main.ts 文件的最顶部，用于快速测试平台功能
Deno.cron("test-cron", "* * * * *", () => {Deno.cron(“test-cron",“ ;* * * * *& ”,()=祝辞{
  console   控制台.log   日志("极简 Cron 测试，当前时间:", new   新    日期Date().toISOString());console   控制台.log   日志("极简 Cron 测试，当前时间:", new   新    日期Date().toISOString());
});

